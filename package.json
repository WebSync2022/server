{
  "name": "server",
  "version": "1.0.0",
  "description": "relay server for WebSync",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "engines": {
    "node": ">= 12.0.0"
  },
  "author": "server.js",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "socket.io": "^4.4.1"
  }
}
